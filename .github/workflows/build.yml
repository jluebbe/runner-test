name: build

on:
  push: {}
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ${{ secrets.build_runs_on || 'ubuntu-latest' }}
    steps:
    - uses: actions/checkout@v3
    - run: echo "Will run on ${{ secrets.build_runs_on || 'ubuntu-latest' }}"
